apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cmclasifierserver
  namespace: gihp4c
spec:
  ingressClassName: traefik
  rules:
    - host: backcmclasifier.gihp4c.edu
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cmclasifierserverservice
                port:
                  number: 80