apiVersion: v1
kind: Service
metadata:
  name: cmclasifierserverservice
  labels:
    app: cmclasifierserver
spec:
  type: ClusterIP
  selector:
    app: test
  ports:
    - name: http
      port: 80
      targetPort: http
